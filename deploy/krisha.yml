version: '3.6'
services:
  krisha:
    container_name: krisha
    restart: always
    image: ${IMAGE}
    networks:
      - dc-net
    environment:
      #CONFIG SERVER
      CONFIG_SERVER_URL: http://config_server
      SERVICE_NAME: krisha
      M_TOKEN: ${M_TOKEN}
      #END CONFIG SERVER

#    ports:
#      - 82:81
networks:
  dc-net:
    external: true